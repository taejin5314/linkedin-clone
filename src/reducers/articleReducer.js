export const initState = {
  loading: false,
};

const aritcleReducer = (state = initState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default aritcleReducer;